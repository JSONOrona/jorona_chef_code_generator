---
driver:
  name: vagrant

provisioner:
  name: chef_zero
  require_chef_omnibus: <%= @chef_omnibus_version %>

platforms:
  - name: centos-6.6

suites:
  - name: default
    run_list:
      - recipe[<%= cookbook_name %>::default]
    encrypted_data_bag_secret_key_path: test/integration/secretfile
    # Uncomment below if testing data bags needed
    # data_bags_path: test/integration/data_bags
    attributes:
